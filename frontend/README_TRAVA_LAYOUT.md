✅ Este ZIP está com o **padrão visual travado**.

- Topo padrão único: `frontend/src/components/SuasTopHeader.jsx`
- Page-header full width: `frontend/src/components/CrasPageHeader.jsx`

Use:
- Validar: `./scripts/check_layout.sh`
- Restaurar: `./scripts/restore_layout.sh`
- Criar wrappers para novos módulos: `./scripts/create_module_wrappers.sh`

Recomendado:
- `git init` e commits frequentes.
