/* HUB — estilo Apple TV (sidebar + prateleiras) */

.hubtv-root{
  min-height: 100vh;
  background: linear-gradient(180deg, rgba(250,250,252,1), rgba(245,246,250,1));
  display: grid;
  grid-template-columns: 280px 1fr;
}

@media (max-width: 980px){
  .hubtv-root{ grid-template-columns: 1fr; }
}

.hubtv-sidebar{
  background: rgba(255,255,255,0.80);
  border-right: 1px solid rgba(0,0,0,0.06);
  padding: 18px;
  position: sticky;
  top: 0;
  height: 100vh;
  box-shadow: 0 20px 70px rgba(0,0,0,0.08);
  backdrop-filter: blur(10px);
  -webkit-backdrop-filter: blur(10px);
}

@media (max-width: 980px){
  .hubtv-sidebar{ position: static; height: auto; border-right:none; border-bottom: 1px solid rgba(0,0,0,0.06); }
}

.hubtv-brand{ display:flex; align-items:center; gap: 12px; margin-bottom: 18px; }
.hubtv-logo{ width: 120px; height:auto; display:block; }

.hubtv-user{
  margin-top: 10px;
  padding-top: 14px;
  border-top: 1px solid rgba(0,0,0,0.06);
  opacity: .75;
  font-weight: 650;
}

.hubtv-nav{ display:grid; gap: 8px; margin-top: 12px; }

.hubtv-navItem{
  display:flex; align-items:center; gap: 10px;
  padding: 10px 12px;
  border-radius: 14px;
  border: 1px solid rgba(0,0,0,0.06);
  background: rgba(255,255,255,0.65);
  cursor: pointer;
  font-weight: 900;
  opacity: .9;
}
.hubtv-navItemActive{
  border: 1px solid rgba(122,92,255,0.28);
  background: rgba(122,92,255,0.10);
}

.hubtv-main{ padding: 22px 22px 40px; }

.hubtv-topbar{
  display:flex;
  justify-content: space-between;
  align-items:center;
  gap: 12px;
  margin-bottom: 18px;
  flex-wrap: wrap;
}

.hubtv-title{ font-size: 34px; font-weight: 980; line-height: 1.05; }
.hubtv-subtitle{ opacity: .72; font-weight: 650; margin-top: 6px; }

.hubtv-actions{ display:flex; gap: 10px; flex-wrap: wrap; }

.hubtv-btn{
  padding: 10px 14px;
  border-radius: 999px;
  border: 1px solid rgba(0,0,0,0.10);
  background: rgba(255,255,255,0.85);
  font-weight: 950;
  cursor:pointer;
  box-shadow: 0 10px 30px rgba(0,0,0,0.08);
  white-space: nowrap;
}
.hubtv-btnPrimary{
  border: 1px solid rgba(122,92,255,0.25);
  background: rgba(122,92,255,0.95);
  color: white;
  box-shadow: 0 18px 50px rgba(122,92,255,0.20);
}

.hubtv-row{ margin-top: 18px; }
.hubtv-rowHeader{
  display:flex;
  justify-content: space-between;
  align-items: baseline;
  gap: 12px;
  margin-bottom: 10px;
}
.hubtv-rowTitle{ font-size: 18px; font-weight: 980; }
.hubtv-rowHint{ opacity: .6; font-weight: 650; }

.hubtv-shelf{
  display:flex;
  gap: 14px;
  overflow-x: auto;
  padding-bottom: 6px;
  scroll-snap-type: x mandatory;
}
.hubtv-shelf::-webkit-scrollbar{ height: 10px; }
.hubtv-shelf::-webkit-scrollbar-thumb{
  background: rgba(0,0,0,0.10);
  border-radius: 999px;
}

.hubtv-card{
  min-width: 260px;
  max-width: 260px;
  scroll-snap-align: start;
  border-radius: 22px;
  padding: 14px;
  border: 1px solid rgba(0,0,0,0.06);
  background: rgba(255,255,255,0.70);
  box-shadow: 0 18px 50px rgba(0,0,0,0.10);
  transition: transform .12s ease, box-shadow .12s ease;
  cursor:pointer;
}
.hubtv-card:hover{
  transform: translateY(-2px);
  box-shadow: 0 22px 60px rgba(0,0,0,0.14);
}

.hubtv-cardTop{
  display:flex;
  justify-content: space-between;
  align-items:center;
  gap: 10px;
  margin-bottom: 10px;
}
.hubtv-cardName{ font-size: 18px; font-weight: 980; }
.hubtv-pill{
  padding: 6px 10px;
  border-radius: 999px;
  border: 1px solid rgba(0,0,0,0.08);
  font-weight: 950;
  white-space: nowrap;
}
.hubtv-pillActive{ background: rgba(120,255,180,.16); color: rgba(0,90,60,1); }
.hubtv-pillSoon{ background: rgba(255,255,255,.70); color: rgba(20,20,30,.55); }

.hubtv-cardDesc{ opacity:.78; font-weight: 650; min-height: 38px; }
.hubtv-cardBtns{ margin-top: 12px; display:flex; gap: 10px; flex-wrap: wrap; }
.hubtv-cardBtn{
  padding: 10px 14px;
  border-radius: 999px;
  border: 1px solid rgba(0,0,0,0.10);
  background: rgba(255,255,255,0.85);
  font-weight: 950;
  cursor:pointer;
}
.hubtv-cardBtnPrimary{
  border: 1px solid rgba(122,92,255,0.25);
  background: rgba(122,92,255,0.95);
  color:white;
}
.hubtv-cardDisabled{ opacity: .55; cursor: default; }
.hubtv-cardDisabled:hover{ transform:none; box-shadow: 0 18px 50px rgba(0,0,0,0.10); }
