/* HUB V2 — NEON/TECH (mais contraste, glow e animação) */

:root{
  --ink: #eaf2ff;
  --ink2: rgba(234,242,255,.78);
  --panel: rgba(10, 16, 28, .46);
  --panel2: rgba(255,255,255,.06);
  --line: rgba(180, 200, 255, .10);
  --ring: rgba(140, 160, 255, .20);

  --p: rgba(168,85,247,.55);
  --c: rgba(56,189,248,.60);
  --b: rgba(59,130,246,.55);
  --v: rgba(139,92,246,.55);
  --i: rgba(99,102,241,.55);
  --m: rgba(236,72,153,.50);
}

.suas2-root{
  min-height: 100vh;
  color: var(--ink);
  background:
    radial-gradient(900px 520px at 16% 12%, rgba(168,85,247,.32), transparent 58%),
    radial-gradient(900px 520px at 86% 12%, rgba(56,189,248,.28), transparent 58%),
    radial-gradient(900px 620px at 50% 96%, rgba(236,72,153,.18), transparent 62%),
    linear-gradient(180deg, #050915 0%, #070b1b 40%, #050915 100%);
  display:flex;
  flex-direction:column;
  overflow:hidden;
}

/* TOP */
.suas2-top{
  display:flex;
  align-items:flex-start;
  justify-content:space-between;
  gap:14px;
  padding: 18px 18px 8px;
}

.suas2-back{
  border: 1px solid rgba(255,255,255,.12);
  background: rgba(255,255,255,.06);
  color: var(--ink);
  padding: 10px 12px;
  border-radius: 14px;
  cursor:pointer;
  box-shadow:
    0 14px 50px rgba(0,0,0,.35),
    0 0 0 1px rgba(56,189,248,.10);
  backdrop-filter: blur(10px);
}
.suas2-back:hover{
  background: rgba(255,255,255,.10);
  box-shadow:
    0 18px 64px rgba(0,0,0,.40),
    0 0 0 1px rgba(56,189,248,.16),
    0 0 30px rgba(56,189,248,.20);
}

.suas2-brand{ max-width: 780px; display:flex; flex-direction:column; align-items:center; gap:6px; }

.suas2-idealLogo{
  height: 36px;
  width: auto;
  object-fit: contain;
  filter: drop-shadow(0 10px 24px rgba(0,0,0,0.30));
}
.suas2-kicker{
  font-weight: 900;
  letter-spacing: .18em;
  font-size: 11px;
  color: rgba(234,242,255,.62);
}
.suas2-title{
  font-size: 38px;
  font-weight: 950;
  letter-spacing: -0.03em;
  margin-top: 6px;
  text-shadow: 0 0 24px rgba(56,189,248,.14);
}
.suas2-sub{
  margin-top: 6px;
  color: rgba(234,242,255,.74);
}

.suas2-right{ width: 90px; }

/* WRAP */
.suas2-wrap{
  position:relative;
  flex: 1;
  display:flex;
  align-items:center;
  justify-content:center;
  padding: 10px 18px 22px;
}

/* GRID + SCAN */
.suas2-grid{
  position:absolute;
  inset:-40px;
  background:
    linear-gradient(rgba(180, 200, 255, .08) 1px, transparent 1px),
    linear-gradient(90deg, rgba(180, 200, 255, .08) 1px, transparent 1px);
  background-size: 46px 46px;
  opacity: .9;
  mask-image: radial-gradient(circle at 50% 44%, rgba(0,0,0,1), rgba(0,0,0,0) 70%);
  pointer-events:none;
}

.suas2-glow{
  position:absolute;
  inset:-120px;
  background:
    radial-gradient(circle at 50% 44%, rgba(56,189,248,.20), transparent 56%),
    radial-gradient(circle at 46% 52%, rgba(168,85,247,.18), transparent 60%),
    radial-gradient(circle at 56% 58%, rgba(236,72,153,.10), transparent 62%);
  filter: blur(14px);
  pointer-events:none;
  opacity: .9;
}

/* ORBIT */
.suas2-orbit{
  position:relative;
  width: min(900px, 92vw, 74vh);
  aspect-ratio: 1/1;
  display:flex;
  align-items:center;
  justify-content:center;
}

/* Ring neon */
.suas2-ring{
  position:absolute;
  inset: 9%;
  border-radius: 999px;
  border: 1px dashed rgba(234,242,255,.20);
  box-shadow:
    0 0 0 18px rgba(168,85,247,.08),
    0 0 0 1px rgba(56,189,248,.14) inset,
    0 0 70px rgba(56,189,248,.14);
  pointer-events:none;
  animation: ringPulse 4.8s ease-in-out infinite;
}

@keyframes ringPulse{
  0%,100% { filter: brightness(1); opacity: .92; }
  50% { filter: brightness(1.25); opacity: 1; }
}

/* CORE */
.suas2-core{
  width: 230px;
  height: 230px;
  border-radius: 999px;
  display:flex;
  flex-direction:column;
  align-items:center;
  justify-content:center;
  background:
    radial-gradient(circle at 30% 25%, rgba(56,189,248,.22), transparent 56%),
    radial-gradient(circle at 70% 70%, rgba(168,85,247,.20), transparent 60%),
    rgba(255,255,255,.06);
  border: 1px solid rgba(255,255,255,.14);
  box-shadow:
    0 26px 90px rgba(0,0,0,.48),
    0 0 70px rgba(56,189,248,.20),
    0 0 90px rgba(168,85,247,.16);
  position:relative;
  backdrop-filter: blur(14px);
}

.suas2-core::before{
  content:"";
  position:absolute;
  inset:-16px;
  border-radius:999px;
  background: conic-gradient(
    from 180deg,
    rgba(56,189,248,0.0),
    rgba(56,189,248,.38),
    rgba(168,85,247,.42),
    rgba(236,72,153,.24),
    rgba(56,189,248,0.0)
  );
  opacity: 1;
  animation: suasSpin 5.2s linear infinite;
  mask: radial-gradient(circle at 50% 50%, transparent 60%, #000 61%);
  pointer-events:none;
}

.suas2-core::after{
  content:"";
  position:absolute;
  inset:-2px;
  border-radius:999px;
  box-shadow:
    0 0 0 1px rgba(56,189,248,.20) inset,
    0 0 0 10px rgba(56,189,248,.05),
    0 0 40px rgba(56,189,248,.18);
  pointer-events:none;
  animation: corePulse 3.6s ease-in-out infinite;
}

@keyframes suasSpin{ to { transform: rotate(360deg); } }
@keyframes corePulse{
  0%,100% { opacity:.75; filter: brightness(1); }
  50% { opacity:1; filter: brightness(1.25); }
}

.suas2-coreLabel{
  font-size: 46px;
  font-weight: 950;
  letter-spacing: -0.03em;
  text-shadow: 0 0 22px rgba(56,189,248,.18);
}
.suas2-coreSub{
  margin-top: 6px;
  font-weight: 800;
  color: rgba(234,242,255,.78);
}

/* SATELLITES */
.suas2-sat{
  --stroke: rgba(234,242,255,.14);
  --glow: rgba(56,189,248,.18);
  position:absolute;
  left: 50%;
  top: 50%;
  width: 270px;
  padding: 12px 12px 11px;
  border-radius: 18px;
  background: linear-gradient(180deg, rgba(255,255,255,.10), rgba(255,255,255,.06));
  border: 1px solid var(--stroke);
  box-shadow:
    0 18px 60px rgba(0,0,0,.48),
    0 0 0 1px rgba(56,189,248,.10) inset,
    0 0 52px var(--glow);
  backdrop-filter: blur(14px);
  cursor:pointer;
  text-align:left;
  transform: translate(-50%, -50%) translate(var(--tx), var(--ty));
  transition: box-shadow .18s ease, background .18s ease, border-color .18s ease, transform .18s ease, filter .18s ease;
}

.suas2-sat::after{
  content:"";
  position:absolute;
  inset:-10px;
  border-radius: 22px;
  background: radial-gradient(circle at 30% 20%, var(--glow), transparent 55%);
  filter: blur(12px);
  opacity: .8;
  pointer-events:none;
}

.suas2-sat:hover{
  background: linear-gradient(180deg, rgba(255,255,255,.14), rgba(255,255,255,.07));
  border-color: rgba(234,242,255,.22);
  box-shadow:
    0 22px 80px rgba(0,0,0,.55),
    0 0 0 1px rgba(56,189,248,.16) inset,
    0 0 74px var(--glow);
  transform: translate(-50%, -50%) translate(var(--tx), var(--ty)) scale(1.03);
  filter: brightness(1.10);
}

.suas2-satTop{
  display:flex;
  align-items:center;
  justify-content:space-between;
  gap:10px;
}
.suas2-satLabel{
  font-weight: 950;
  font-size: 15px;
  color: rgba(234,242,255,.96);
}
.suas2-satDesc{
  margin-top: 6px;
  color: rgba(234,242,255,.74);
  font-size: 12px;
  line-height: 1.25;
}

/* pills */
.suas2-pill{
  border-radius: 999px;
  padding: 5px 10px;
  font-size: 11px;
  font-weight: 950;
  border: 1px solid rgba(234,242,255,.16);
  background: rgba(255,255,255,.06);
  color: rgba(234,242,255,.86);
  white-space: nowrap;
}
.suas2-pill--on{
  background: rgba(34,197,94,.14);
  border-color: rgba(34,197,94,.28);
  color: rgba(210,255,225,.92);
}
.suas2-pill--soon{
  background: rgba(148,163,184,.14);
  border-color: rgba(148,163,184,.22);
  color: rgba(234,242,255,.72);
}

/* disabled: menos "apagado" e mais "desligado tech" */
.suas2-sat--disabled{
  opacity:.86;
  cursor:not-allowed;
  filter: saturate(.85);
}

/* tones */
.suas2-sat[data-tone="purple"]{ --stroke: rgba(168,85,247,.28); --glow: rgba(168,85,247,.22); }
.suas2-sat[data-tone="blue"]{ --stroke: rgba(59,130,246,.26); --glow: rgba(59,130,246,.20); }
.suas2-sat[data-tone="violet"]{ --stroke: rgba(139,92,246,.26); --glow: rgba(139,92,246,.20); }
.suas2-sat[data-tone="indigo"]{ --stroke: rgba(99,102,241,.26); --glow: rgba(99,102,241,.20); }
.suas2-sat[data-tone="pink"]{ --stroke: rgba(236,72,153,.24); --glow: rgba(236,72,153,.18); }
.suas2-sat[data-tone="cyan"]{ --stroke: rgba(56,189,248,.30); --glow: rgba(56,189,248,.26); }

/* Active Pop Rua: pulso neon */
.suas2-sat[data-tone="cyan"][data-status="ativo"]{
  animation: popPulse 3.2s ease-in-out infinite;
  box-shadow:
    0 22px 80px rgba(0,0,0,.55),
    0 0 0 1px rgba(56,189,248,.22) inset,
    0 0 92px rgba(56,189,248,.30);
}
@keyframes popPulse{
  0%,100% { transform: translate(-50%, -50%) translate(var(--tx), var(--ty)) scale(1); }
  50% { transform: translate(-50%, -50%) translate(var(--tx), var(--ty)) scale(1.02); }
}

/* LISTA mobile */
.suas2-list{ display:none; width: min(720px, 92vw); }
.suas2-card{
  --stroke: rgba(234,242,255,.14);
  --glow: rgba(56,189,248,.18);
  width: 100%;
  margin: 10px 0;
  padding: 14px;
  border-radius: 18px;
  background: linear-gradient(180deg, rgba(255,255,255,.10), rgba(255,255,255,.06));
  border: 1px solid var(--stroke);
  box-shadow:
    0 18px 60px rgba(0,0,0,.48),
    0 0 44px var(--glow);
  text-align:left;
  cursor:pointer;
  backdrop-filter: blur(14px);
}
.suas2-cardTop{ display:flex; justify-content:space-between; gap:10px; align-items:center; }
.suas2-cardLabel{ font-weight: 950; font-size: 16px; color: rgba(234,242,255,.96); }
.suas2-cardDesc{ margin-top:6px; color: rgba(234,242,255,.74); font-size: 13px; }
.suas2-card--disabled{ opacity:.86; cursor:not-allowed; filter:saturate(.85); }
.suas2-card[data-tone="purple"]{ --stroke: rgba(168,85,247,.28); --glow: rgba(168,85,247,.18); }
.suas2-card[data-tone="blue"]{ --stroke: rgba(59,130,246,.26); --glow: rgba(59,130,246,.16); }
.suas2-card[data-tone="violet"]{ --stroke: rgba(139,92,246,.26); --glow: rgba(139,92,246,.16); }
.suas2-card[data-tone="indigo"]{ --stroke: rgba(99,102,241,.26); --glow: rgba(99,102,241,.16); }
.suas2-card[data-tone="pink"]{ --stroke: rgba(236,72,153,.24); --glow: rgba(236,72,153,.14); }
.suas2-card[data-tone="cyan"]{ --stroke: rgba(56,189,248,.30); --glow: rgba(56,189,248,.22); }

/* toast */
.suas2-toast{
  position: fixed;
  left: 50%;
  bottom: 16px;
  transform: translateX(-50%);
  background: rgba(5,9,21,.86);
  border: 1px solid rgba(234,242,255,.14);
  color: #fff;
  padding: 10px 14px;
  border-radius: 14px;
  box-shadow: 0 22px 80px rgba(0,0,0,.55);
  z-index: 9999;
}

@media (max-width: 860px){
  .suas2-title{ font-size: 30px; }
  .suas2-orbit{ display:none; }
  .suas2-list{ display:block; }
}


/* === CYBER HUD ADDON === */

/* === CYBER HUD ADDON === */
/* Scanlines + noise + HUD corners + flicker sutil (mais cyber sem atrapalhar leitura) */

.suas2-root{
  position: relative;
}

.suas2-root::before{
  content:"";
  position: fixed;
  inset: 0;
  pointer-events:none;
  /* ruído sutil + vinheta tech */
  background:
    radial-gradient(circle at 50% 40%, rgba(56,189,248,.10), transparent 58%),
    radial-gradient(circle at 40% 55%, rgba(168,85,247,.09), transparent 62%),
    radial-gradient(circle at 60% 70%, rgba(236,72,153,.06), transparent 62%),
    repeating-linear-gradient(0deg, rgba(255,255,255,.02) 0px, rgba(255,255,255,.02) 1px, transparent 2px, transparent 6px);
  mix-blend-mode: screen;
  opacity: .55;
  filter: contrast(1.08) saturate(1.08);
  animation: hudBreath 6.5s ease-in-out infinite;
  z-index: 1;
}

.suas2-root::after{
  content:"";
  position: fixed;
  inset: 0;
  pointer-events:none;
  /* scanline “varrendo” */
  background:
    linear-gradient(180deg, transparent 0%, rgba(56,189,248,.12) 48%, transparent 100%);
  opacity: .35;
  filter: blur(0px);
  transform: translateY(-120%);
  animation: scanSweep 5.2s linear infinite;
  z-index: 2;
}

/* HUD corners (moldura) */
.suas2-wrap{
  position: relative;
}

.suas2-wrap::before{
  content:"";
  position:absolute;
  inset: 10px;
  border-radius: 22px;
  border: 1px solid rgba(234,242,255,.10);
  box-shadow:
    0 0 0 1px rgba(56,189,248,.08) inset,
    0 0 60px rgba(56,189,248,.10),
    0 0 80px rgba(168,85,247,.06);
  pointer-events:none;
  opacity: .95;
  z-index: 3;
}

.suas2-wrap::after{
  content:"";
  position:absolute;
  inset: 10px;
  border-radius: 22px;
  pointer-events:none;
  /* cantos HUD */
  background:
    linear-gradient(90deg, rgba(56,189,248,.45), rgba(56,189,248,0)) left top/110px 2px no-repeat,
    linear-gradient(180deg, rgba(56,189,248,.45), rgba(56,189,248,0)) left top/2px 110px no-repeat,

    linear-gradient(180deg, rgba(168,85,247,.42), rgba(168,85,247,0)) right top/2px 110px no-repeat,
    linear-gradient(270deg, rgba(168,85,247,.42), rgba(168,85,247,0)) right top/110px 2px no-repeat,

    linear-gradient(0deg, rgba(236,72,153,.30), rgba(236,72,153,0)) left bottom/2px 110px no-repeat,
    linear-gradient(90deg, rgba(236,72,153,.30), rgba(236,72,153,0)) left bottom/110px 2px no-repeat,

    linear-gradient(270deg, rgba(56,189,248,.30), rgba(56,189,248,0)) right bottom/110px 2px no-repeat,
    linear-gradient(0deg, rgba(56,189,248,.30), rgba(56,189,248,0)) right bottom/2px 110px no-repeat;
  opacity: .80;
  filter: drop-shadow(0 0 12px rgba(56,189,248,.14));
  animation: hudFlicker 4.6s ease-in-out infinite;
  z-index: 4;
}

/* título com glow e micro-flicker */
.suas2-title{
  text-shadow:
    0 0 26px rgba(56,189,248,.18),
    0 0 44px rgba(168,85,247,.10);
  animation: titleFlicker 8.5s ease-in-out infinite;
}

/* ring mais “energizado” */
.suas2-ring{
  box-shadow:
    0 0 0 18px rgba(168,85,247,.10),
    0 0 0 1px rgba(56,189,248,.18) inset,
    0 0 90px rgba(56,189,248,.18),
    0 0 120px rgba(168,85,247,.12);
}

/* poprua com brilho mais “ativo” */
.suas2-sat[data-tone="cyan"][data-status="ativo"]{
  box-shadow:
    0 22px 90px rgba(0,0,0,.58),
    0 0 0 1px rgba(56,189,248,.26) inset,
    0 0 120px rgba(56,189,248,.34);
}

/* animações */
@keyframes scanSweep{
  0% { transform: translateY(-140%); opacity: .10; }
  8% { opacity: .35; }
  55% { opacity: .30; }
  100% { transform: translateY(140%); opacity: .08; }
}

@keyframes hudBreath{
  0%,100% { opacity: .48; filter: contrast(1.06) saturate(1.06); }
  50% { opacity: .62; filter: contrast(1.12) saturate(1.12); }
}

@keyframes hudFlicker{
  0%,100% { opacity: .78; }
  48% { opacity: .86; }
  52% { opacity: .62; }
  56% { opacity: .88; }
}

@keyframes titleFlicker{
  0%,100% { opacity: 1; }
  49% { opacity: .98; }
  50% { opacity: .92; }
  51% { opacity: 1; }
}

/* no mobile: menos scan (pra não ficar pesado) */
@media (max-width: 860px){
  .suas2-root::before{ opacity: .38; }
  .suas2-root::after{ opacity: .22; }
}


/* === FINAL TUNE V1 === */
/* Clareia, reduz HUD “moldura”, melhora contraste e deixa EM BREVE com cara de roadmap */

.suas2-root{
  background:
    radial-gradient(900px 520px at 16% 12%, rgba(168,85,247,.26), transparent 58%),
    radial-gradient(900px 520px at 86% 12%, rgba(56,189,248,.22), transparent 58%),
    radial-gradient(900px 620px at 50% 96%, rgba(236,72,153,.14), transparent 64%),
    linear-gradient(180deg, #070b20 0%, #0a1030 45%, #070b20 100%);
}

/* HUD overlay menos dominante */
.suas2-root::before{ opacity: .34 !important; filter: contrast(1.05) saturate(1.06) !important; }
.suas2-root::after{ opacity: .18 !important; animation-duration: 6.6s !important; }

/* Moldura HUD bem mais sutil (tava chamando atenção demais) */
.suas2-wrap::before{
  opacity: .55 !important;
  border-color: rgba(234,242,255,.07) !important;
  box-shadow:
    0 0 0 1px rgba(56,189,248,.05) inset,
    0 0 38px rgba(56,189,248,.07),
    0 0 52px rgba(168,85,247,.05) !important;
}
.suas2-wrap::after{
  opacity: .46 !important;
  filter: drop-shadow(0 0 8px rgba(56,189,248,.10)) !important;
}

/* Cards com mais legibilidade (premium) */
.suas2-sat{
  background: linear-gradient(180deg, rgba(255,255,255,.14), rgba(255,255,255,.08)) !important;
  border-color: rgba(234,242,255,.18) !important;
  box-shadow:
    0 18px 60px rgba(0,0,0,.44),
    0 0 0 1px rgba(56,189,248,.10) inset,
    0 0 50px var(--glow) !important;
}
.suas2-sat:hover{
  background: linear-gradient(180deg, rgba(255,255,255,.18), rgba(255,255,255,.10)) !important;
  box-shadow:
    0 22px 78px rgba(0,0,0,.52),
    0 0 0 1px rgba(56,189,248,.16) inset,
    0 0 70px var(--glow) !important;
}

/* “EM BREVE” com cara de roadmap (não cinza morto) */
.suas2-pill--soon{
  background: rgba(56,189,248,.10) !important;
  border-color: rgba(56,189,248,.22) !important;
  color: rgba(234,242,255,.84) !important;
  box-shadow: 0 0 22px rgba(56,189,248,.10) !important;
}

/* Desabilitado menos apagado */
.suas2-sat--disabled{
  opacity: .92 !important;
  filter: saturate(.95) !important;
}
.suas2-sat--disabled .suas2-satLabel{ color: rgba(234,242,255,.92) !important; }
.suas2-sat--disabled .suas2-satDesc{ color: rgba(234,242,255,.74) !important; }

/* Núcleo um pouco mais “energizado” */
.suas2-core{
  background:
    radial-gradient(circle at 30% 25%, rgba(56,189,248,.22), transparent 56%),
    radial-gradient(circle at 70% 70%, rgba(168,85,247,.20), transparent 60%),
    rgba(255,255,255,.08) !important;
}

